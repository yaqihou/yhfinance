from typing import Optional

from .loader import TickerData


class Analyzer:

    def __init__(self, ticker_data: Optional[TickerData] = None):
        self.ticker_data: TickerData
        pass


    def load_data(self, ticker_data: TickerData):
        self.ticker_data = ticker_data


    def preprocess(self):

        # Common data clean
        self.df